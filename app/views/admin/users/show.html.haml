= @user.name
%br
= @user.email
%br
Account created on #{@user.created_at.strftime('%m/%d/%Y')}
%br
Pictures count: #{@pictures_count}
%br
Snapshots count: #{@snapshots_count}
%br
Snapshots last week: #{@recent_snapshots_count}
%br
= link_to 'Gallery', gallery_path(@user.galleries.first)
%br
Recent snapshots:
%br
- @recent_snapshots.each do |snapshot|
  = render 'snapshots/snapshot_thumbnail', snapshot: snapshot
