%h1
  Listing palettes

#notice
  = notice

%br

- @palettes.each do |palette|

  %h1
    = link_to palette.name, palette_path(palette)

  = render 'palette_colors_blocks', palette: palette

  %br
  %br
%br
%br
- if user_signed_in?
  = link_to 'New Palette', new_palette_path
