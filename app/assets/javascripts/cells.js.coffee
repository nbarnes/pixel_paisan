$ ->

  # class Cell
  #   constructor: (@x, @y) ->

  #   trigger: () ->
  #     target_x = cell_x_to_x ( @x )
  #     target_y = cell_y_to_y ( @y )
  #     context.fillRect(target_x, target_y, cell_size(), cell_size())

  # cell_x_to_x = (cell_x) ->
  #   cell_x * cell_size()

  # cell_y_to_y = (cell_y) ->
  #   cell_y * cell_size()